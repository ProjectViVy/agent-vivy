# T0 — Reconcile baseline and validation prerequisites Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** Reconcile baseline and validation prerequisites.
**Epic:** Core-0. **Requirements:** R0.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D3](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** e2bfc333f857ddf36f828e3bb17e06e7c6a3e8cf (code base a2d2b5912e8dd93668aae787b198441d8100a969).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

No predecessor; this is the read-only baseline and prerequisite audit.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Read AGENTS.md, ui/AGENTS.md, spec; docs/architecture/PLAN-GOAL-PREDESIGN.md; internal/storage/contracts.go; recipes/minimal.vivy.yml, recipes/default.vivy.yml; justfile. Record actual evidence in docs/logs/2026-09-21-session-continuity-implementation/verification.md when execution begins.

**Interfaces:** No new interface; establishes the migration owner and tool-registration path for all tasks.

- [ ] Inspect git status/current main and open #45 state. Use its centralized migration owner after merge, or obtain explicit authorization before integrating its reviewed storage-only commit onto the implementation branch. Do not duplicate its SQL loader. If integration is unavailable, report the storage dependency and complete only non-DDL work.
- [ ] Read pinned Eino source or the verified pinned-source evidence in SC-D3. Reuse toolAdapter.Info/InvokableRun, Runner.Run/Resume and ContextHost; do not add a retriever or second loop. Preserve existing fixed-core/deferred-tool discovery instead of hard-wiring five new always-visible tools.
- [ ] Establish build prerequisites and run `just ci` once before product edits. Record existing failures separately. Current planning environment lacks just; this is a known prerequisite, not permission to mark CI green.
- [ ] Record the exact centralized migration manifest files and next free migration number from that integrated tree. Name paired files with suffix `_session_continuity.sql`; numbering must be allocated from the actual manifest, not this older baseline.
- [ ] Confirm minimal.vivy.yml currently has no GUI module; create the Lite GUI artifact as recipes/lite.vivy.yml in T12. The existing minimal.vivy.yml is a baseline recipe identifier, not the name of the GUI product; do not rename or alias it as Headless.

## Boundary details

Record #45's current merged/head/base revisions using read-only access. Integrating an unmerged PR requires separate authorization; do not infer it from this planning request. Until the migration owner is available, T2/T4 remain blocked. Inspect all message insertion sites with `rg -n 'INSERT.*messages|AppendMessage|AppendMessageIfAbsent' internal/storage`; include journal and fork writers. No product implementation during T0.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
