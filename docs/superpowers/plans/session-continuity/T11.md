# T11 — Additive delivery groups and GUI actions Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** Additive delivery groups and GUI actions.
**Epic:** Core-3. **Requirements:** R3,R5.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D4](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** a8d361b0244a1c40be513622bbdaebb5c9d40014 (current main; SC-P5 refresh).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

Consume accepted outputs from [T10](T10.md), [T8](T8.md) as described in the index.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Create ui/src/components/chat/DeliverableCard.tsx/test, ui/src/lib/deliverable-download.ts/test; extend files/FilesPanel.tsx, run-rows.ts/test, api.ts, store.ts/test and chat rendering; locales.

**Interfaces:** New run-row kind deliverables contains immutable DeliverySet. DeliverableCard accepts set + availabilityByItem + actions; summary reads same store data. `downloadDeliverable(item, rpc, signal)` owns sequential chunk fetching and object URL cleanup; rpc adapter calls ledger methods. Function resolves after EOF, throws typed reason on mismatch/abort; no partial download is labeled complete.

- [ ] Add real React DOM test and download unit test:

```ts
expect(container.querySelectorAll('[data-delivery-set]')).toHaveLength(2);
expect(container.textContent).toContain('2 files delivered');
expect(container.textContent).not.toContain('Verification passed');
expect(createObjectURL).not.toHaveBeenCalled(); // interrupted before EOF
expect(closeTransfer).toHaveBeenCalledWith(activeTransferId);
```

- [ ] Run `pnpm test src/components/chat/DeliverableCard.test.tsx src/lib/deliverable-download.test.ts src/lib/run-rows.test.ts`; observe red behavior.
- [ ] Implement immutable chronological groups, each failed entry visible, unchecked initial availability, bounded text preview, safe attachment downloads, changed/missing explanatory states and independent run-cancel status. Sidebar links focus the original group; it is separate from modified-file list. Preserve older same-path delivery groups. Use actual MIME/type for deciding preview; HTML/SVG never execute in app origin.
- [ ] Test sequential transfer cancel/retry/disconnect, base64/frame mismatch, EOF/hash consistency, object URL revoke on completion/unmount, one active transfer per connection, no success card before committed event, long paths/CJK, screen-reader labels, keyboard actions and mobile overflow. Combined E2E must verify actual bytes, not only mocked click callbacks.
- [ ] Run tests/typecheck; commit `feat(ui): present explicit additive file delivery groups`.

## Boundary details

Any new exported API operation or store field must also update sdk/ui/src/module.ts and sdk/ui/src/module.test.ts and pass ui/src/lib/ui-sdk-face-compat.test.ts. This is existing Face-contract alignment, not a new plugin feature. Reuse the T7 shared submission owner; do not redefine its DTO.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
