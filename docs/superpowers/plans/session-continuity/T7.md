# T7 — History picker, composer and queue Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** History picker, composer and queue.
**Epic:** Core-2. **Requirements:** R1,R2,R5.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D3](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** e2bfc333f857ddf36f828e3bb17e06e7c6a3e8cf (code base a2d2b5912e8dd93668aae787b198441d8100a969).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

Consume accepted outputs from [T5](T5.md) as described in the index.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Extend ui/src/lib/api.ts, store.ts, ChatInput.tsx, ChatView.tsx and their tests; create ui/src/components/chat/HistoryPicker.tsx/test, ContextReferenceChip.tsx/test; use existing dialog/sheet/checkbox/scroll-area components; update en.ts/zh.ts.

**Interfaces:** Define `TurnSubmission` in api.ts with existing text/mode/face/attachments/thinking plus `continuity: {request_id,references,history_scope}`. Change startTurn and store send/enqueue internally to this typed object; update all callers atomically. HistoryPicker props: destinationSessionId, open, onOpenChange, onAttach(ReferenceSelection,allowFurtherReading). Chip props: preview, onView, onRemove. Draft fields are session-bound ephemeral state, not an ACL.

- [ ] Use existing createRoot/act Vitest pattern (no new testing library). In store.test.ts record the actual turn/start request after queue drain:

```ts
expect(sent.continuity.references).toEqual(queued.continuity.references);
expect(sent.continuity.history_scope.session_ids).toEqual([]);
expect(sent.continuity.request_id).toBe(queued.continuity.request_id);
expect(useVivyStore.getState().queuedMessages).toHaveLength(1); // stale head retained
```

- [ ] Run from ui: `pnpm test src/lib/store.test.ts src/components/chat/HistoryPicker.test.tsx src/components/chat/ContextReferenceChip.test.tsx`; confirm fail before behavior exists.
- [ ] Implement SC-D3 §13.1–13.3 layout/state flow. Search only on submit; increment a local request-generation counter and discard responses not matching current destination/query. Preview before Attach; add broader scope only when checkbox is selected. Reuse persisted scope display from backend for accepted tasks. Queue copies full submission; conflict stops drain with item retained; remove can resume later items. Unknown response retries same request ID. Failed sends retain text/images/references.
- [ ] Add tests for unchecked further-read, explicit opt-in, scope-only submission, source/query response arriving after session switch, narrowing limit, cancelled picker, both image + reference, draft reset, stale queued item edit, double Send, keyboard focus, 320/768/1280 widths and en/zh. Never auto-retry stale content with a fresh digest.
- [ ] Run `pnpm typecheck` and targeted tests; commit `feat(ui): compose tasks with explicit history references`.

## Boundary details

Inspect sdk/ui/src/module.ts FaceClientAPI.startTurn and FaceStoreState.enqueueMessage before changing signatures: ui/src/lib/ui-sdk-face-compat.test.ts checks exact API/store compatibility. Include sdk/ui/src/module.ts, sdk/ui/src/module.test.ts and the compatibility test in the same atomic change; export the shared structural submission type from the existing SDK owner, with the host API reusing it. Do not introduce a new Port or a second manually maintained submission shape. Preserve existing text/mode/face/attachments/thinking behavior and update all repository callers. Public signature divergence from this approved typed-submission change requires escalation.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
