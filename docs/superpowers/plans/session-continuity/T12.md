# T12 — End-to-end coding acceptance and Lite Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** End-to-end coding acceptance and Lite.
**Epic:** Core-4. **Requirements:** R1,R2,R3,R4,R5,R6.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D4](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** a8d361b0244a1c40be513622bbdaebb5c9d40014 (current main; SC-P5 refresh).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

Consume accepted outputs from [T11](T11.md) as described in the index.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Create internal/app/continuity_e2e_test.go, ui/e2e/session-continuity.spec.ts, ui/playwright.continuity.config.ts and ui/e2e/continuity-setup.ts; update only required recipe composition for recipes/lite.vivy.yml; record implementation logs. Use existing generated Assembly path and SDK conformance tests.

**Interfaces:** Real app/RPC/services/stores from previous tasks, scripted test model and isolated temporary workspace. Do not use demo-api. Test config enables the actual five continuity tools and filesystem/shell tools; baseline E2E config currently enables only echo_info/write_note/ask_user and is insufficient.

- [ ] Write a failing full loop fixture: source A has rationale/message/tool result; unrelated C contains a sentinel; B selects A for further read, model searches/reads/traces, attaches, edits source, invokes test command, creates report and archive through shell, invokes present_files. Capture actual model requests and assert C sentinel absent.

```ts
await expect(page.getByText('Source unavailable; saved excerpt retained')).toBeVisible();
const downloadReady = page.waitForEvent('download');
await page.getByRole('button', { name: 'Download report.txt' }).click();
const download = await downloadReady;
expect(await sha256DownloadedFile(download)).toBe(expectedPresentedDigest);
```

Implementation of sha256DownloadedFile belongs in continuity-setup.ts using Playwright Download.path and Node crypto.createHash; throw if path/failure is absent. Set download listener before clicking Download in the test so the event cannot race the waiter.

- [ ] Run `go test ./internal/app -run TestContinuityCodingLoop -count=1`; run UI `pnpm exec playwright test --config playwright.continuity.config.ts`. Observe missing integration; fix only contract wiring failures, no test-only product bypass.
- [ ] New Playwright config starts ordinary backend + Vite split pair, baseURL http://127.0.0.1:3015; backend uses isolated temp config/workspace/database, never tenant data. Reuse existing E2E helpers for ports/config where safe but do not mutate every existing E2E test to use this scenario. Use installed Go executable override on Linux, not existing Windows default. Configure deterministic loopback model endpoint in test harness; live provider smoke is additional and reported separately when available.
- [ ] Exercise restart/reconnect, source deletion, changed file, partially failed group, failed queued reference, source scope rejection and same-path second group. Assert no duplicate reference/group after replay; verify body remains accessible while source jump is disabled. Exercise 320/768/1280 widths and keyboard flow using screenshot evidence plus DOM/action checks.
- [ ] Retain the existing embedded web gateway/UI; create `recipes/lite.vivy.yml` only with verified real modules/extensions. Prove the GUI with artifact inspection plus HTTP/UI smoke. Do not invent `vivy/web` or treat `vivy/face-host` as a Web Face provider. Run SDK verify/pack/inspect-artifact and physical omission/conformance tests. Confirm channels/persona/memory/SSH/image-offload absent and history/reference/present still discoverable. Default generation also passes; do not replace its recipe.
- [ ] Run `just ci`; run first-party Postgres integration with configured VIVY_POSTGRES_TEST_DSN and SQLite. Record failing/skipped gates honestly. Collect tool-call transcript IDs, source refs, event counts, artifact digests, GUI evidence and inspected generation IDs in implementation verification.md. Only then propose closing acceptance checkboxes in #51 and updating #46/#49; publishing requires user authorization.
- [ ] Commit with `test(continuity): verify the complete coding delivery loop` plus actual integration corrections and evidence. Stop additional testing once these required gates and concrete risks are covered.

## Boundary details

T12 transitively consumes T6 and T8 through T11; verify their accepted evidence explicitly without duplicating transitive DAG edges. Read the repository vivy-plugin skill before composing Lite. Pack to a fresh output directory and inspect the same artifact; disabled configuration is not physical omission. No publication or issue closure is authorized by this plan.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
