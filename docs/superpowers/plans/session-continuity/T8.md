# T8 — Reference details and model history timeline Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** Reference details and model history timeline.
**Epic:** Core-2. **Requirements:** R1,R2,R4,R5.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D4](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** a8d361b0244a1c40be513622bbdaebb5c9d40014 (current main; SC-P5 refresh).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

Consume accepted outputs from [T6](T6.md), [T7](T7.md) as described in the index.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Create ui/src/components/chat/ReferenceDetail.tsx/test; extend run-rows.ts/test, store.ts/test, ChatView.tsx/test, ToolRow.tsx/tool-presentation.ts and ui/src/components/chat/RunInspector.tsx; RPC reference/get projection from T6; locales.

**Interfaces:** Add RunRow kind context_reference identified by event ref, not tool-result prose. ReferenceDetail consumes ContextReference + source_status + feed_status. Source jump reuses history/read; does not require changing active destination session.

- [ ] Assert reconnection and status axes with actual row-fold test fixtures:

```ts
const rows = foldContinuityRows([attached, attached]);
expect(rows.filter(r => r.kind === 'context_reference')).toHaveLength(1);
expect(view.source_status).toBe('unavailable');
expect(view.feed_status).toBe('elided_budget');
expect(view.snapshot.items.length).toBeGreaterThan(0);
```

Fixture attached is a literal domain event with stable run_id/seq/reference ID; view is the returned reference/get DTO. Define foldContinuityRows in run-rows.ts as the typed continuity-event projection and merge it into the existing row builder, with shared event-key deduplication.

- [ ] Run `pnpm test src/lib/run-rows.test.ts src/components/chat/ReferenceDetail.test.tsx src/components/chat/ChatView.test.tsx` and observe failure.
- [ ] Implement neutral imported-data card; show saved excerpt separately from live source status. Humanize history tool labels without hiding expandable arguments/results. Keep source-ID details collapsed. Disable source jump only when unavailable/denied; keep saved copy readable. Existing generic tool row remains fallback for unknown event version.
- [ ] Test out-of-order subscription/live + replay union, source deletion vs corruption vs budget elision, historical card after restart, malicious markdown/link schemes escaped through existing renderer, Escape/focus return and narrow layout. No duplicate card from tool.finished.
- [ ] Run targeted tests/typecheck; commit `feat(ui): inspect referenced history with provenance`.

## Boundary details

Wire reference/get in internal/rpc/references.go and its tests; T6 owns the runtime projection and T8 owns this GUI-facing dispatch/adapter integration. Model-provided descriptions and markdown remain untrusted text. Merge replay/live events by stable identity before producing rows; never insert a second card from the corresponding tool result.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
