# T10 — Presentation tool, RPC and durable discovery Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** Presentation tool, RPC and durable discovery.
**Epic:** Core-3. **Requirements:** R1,R3,R4.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D4](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** a8d361b0244a1c40be513622bbdaebb5c9d40014 (current main; SC-P5 refresh).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

Consume accepted outputs from [T9](T9.md), [T3](T3.md) as described in the index.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Create internal/tools/present.go/test, internal/rpc/deliverables.go/test; extend tools.go, app assembly wiring, rpc control dispatch; add runtime delivery replay integration tests.

**Interfaces:** `NewPresentFiles(DeliverableOperations)` registers present_files as effectful metadata tool through existing policy. RPC deliverables/list/get/read/close uses trusted connection identity. Read errors return stable reasons changed/missing/workspace_unavailable/forbidden, mapped to common status envelope. No arbitrary HTTP transfer route.

- [ ] Add tool test schema and RPC roundtrip: input has files path/description only; reject url/upload/workspace_id/actor and unknown keys. Scripted model must invoke present_files and receive the committed set ID. Recovery test consumes persisted event after tool-finished loss.

```go
if committed.ID != afterRestart.ID { t.Fatal("lost durable set") }
if binaryDigest != committed.Items[0].SHA256 { t.Fatal("downloaded wrong bytes") }
if bypassedPolicy { t.Fatal("presentation bypassed tool host") }
```

- [ ] Run `go test ./internal/tools ./internal/rpc ./internal/runtime -run 'TestPresent|TestDeliverable' -count=1`; verify missing behavior then implement.
- [ ] Wire existing toolAdapter and discovery; bounded List/Get reads only relevant domain events through SQL, excludes hidden rewound destination turns. Add history artifact filter by actual delivery ID relation now; task filter remains unsupported without durable linkage. Receipt replay fetches authoritative event result; no model text parser becomes a second presentation channel.
- [ ] Verify all-failed set, approval deny, terminal race, event-size ceiling, restart list, owner mismatch, forged transfer handle, raw path rejection, scope bypass attempts and plaintext/binary roundtrip using actual RPC transport.
- [ ] Commit `feat(deliverables): expose explicit model and GUI delivery operations`.

## Boundary details

T3 is an explicit predecessor because this Story enables the artifact filter in the already implemented HistoryService and capabilities response. Add internal/runtime/history_service.go and history_service_test.go to its permitted integration edits. A delivery ID filter follows actual set/item provenance, not filename text; unrelated and out-of-scope IDs disclose no metadata.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
