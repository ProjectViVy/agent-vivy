# T2 — Stable, bounded history storage on both backends Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans after user approval of this package and execution method, or superpowers:subagent-driven-development if explicitly selected.

**Goal:** Stable, bounded history storage on both backends.
**Epic:** Core-1. **Requirements:** R1,R4.
**Architecture:** Reuse the approved host operations and single Journal; implement only this Story's boundary.
**Tech Stack:** Existing Go/Eino, SQLite/PostgreSQL and React/TypeScript as applicable; no new production dependency.
**Spec:** [SC-D3](../../specs/2026-09-21-session-continuity-design.md).
**Baseline:** e2bfc333f857ddf36f828e3bb17e06e7c6a3e8cf (code base a2d2b5912e8dd93668aae787b198441d8100a969).
**Index:** [Status, predecessors and evidence](index.md). Read the [shared ledger](../2026-09-21-session-continuity.md#shared-contract-ledger) before implementation.

## Global Constraints

All [global constraints](../2026-09-21-session-continuity.md#global-constraints) apply. Lite retains GUI; Headless is a presentation mode. New filenames below are proposed, not implemented. Paths abbreviated by package resolve under the named existing package, never a new root runtime directory.

## Review Focus

Apply the [five shared failure classes](../2026-09-21-session-continuity.md#review-focus) through this Story's tests below. A planned assertion is not passing evidence.

## Prerequisites, scope and handoff

Consume accepted outputs from [T1](T1.md) as described in the index.
Do not begin product edits until the index releases this Story. Scope is the files/contracts and observable outcome below; broader architecture, public authorization semantics, unrelated refactors, remote writes and other Stories' implementation are excluded.
Return the changed-file list, exact commands/results, failure/skip reasons and accepted predecessor revisions. Stop on incompatible contracts rather than choosing a new architecture.

## Implementation tasks

**Files:** Create internal/storage/history.go; internal/storage/sqlite/history.go, history_test.go; matching postgres/history.go, history_test.go; internal/storage/conformance/history.go. Modify both messages.go, sessions.go, history_mutations.go and centralized paired migration/manifest files allocated in T0.

**Interfaces:** Implement HistoryQueryStore and ordering ledger. No public model-facing raw records. Conformance helper `RunHistoryQuerySuite(t,*backend)` uses the same backend operations on SQLite and configured Postgres; fixture insertion uses existing CreateSession/AppendMessage/CreateRun/Journal.Append.

- [ ] Add backend test sequence as actual test helper `assertHistoryCutStable(t, storage.HistoryQueryStore, appendMessage func(domain.Message))`; insert B:m1 and B:m2 with identical CreatedAt, capture cut, append B:m3 with earlier timestamp, page at limit1. Assert m1/m2 appear once and m3 never appears in the captured cut. Repeat with delayed assistant projection and a second session.

```go
// Inside the shared helper after obtaining pages:
if strings.Join(ids, ",") != "m1,m2" { t.Fatalf("unstable cut: %v", ids) }
if len(seen) != len(ids) { t.Fatal("duplicate history result") }
if page.BytesInspected > 4<<20 { t.Fatal("unbounded payload scan") }
```

- [ ] Run `go test ./internal/storage/... -run 'TestHistory|TestContinuityMigration' -count=1`; record red state; Postgres skip is not pass.
- [ ] Implement ordering migration and every message writer under transaction; parameterize source-ID predicates before payload reads. Cursor cut bounds by insertion ceilings and event seq, not timestamp. Query limit+1 for has-more only within scan ceiling. Detect oversized raw record before allocating it; emit unavailable/truncated metadata and advance cursor so one record cannot starve progress.
- [ ] Test reopen/legacy migration, upgrade idempotency, source deletion between pages, >256-run selection, cancellation, 2,000-record budget, malicious source IDs, empty-but-scan-incomplete pages, SQLite/Postgres parity and rebuild ordering. Validate migration manifest/checksum using the centralized owner's tests.
- [ ] Commit with `feat(storage): add bounded stable history queries`.

## Boundary details

Position allocation must cover AppendMessage, AppendMessageIfAbsent, atomic admission, edit and fork writers. A duplicate deterministic projection must return the existing row without changing its position. Capture message and run ceilings in one consistent read transaction; a later page still applies current deletion/rewind visibility. Reserve migration numbering only from the integrated owner. No new inline DDL.

## Acceptance and evidence

- [ ] The Story outcome is demonstrated by the listed tests/checks against real owning seams.
- [ ] Tests listed here have actual results; unavailable Postgres/browser/CI prerequisites remain unverified.
- [ ] Contract changes are propagated to consumers before dependent work begins.
- [ ] Evidence is recorded in the implementation iteration log and linked from the index.
- [ ] No unrelated files, hidden authority expansion or duplicate source of truth were introduced.

Rollback: revert only this Story's implementation commit when safe; database changes follow the migration owner's forward-repair policy. Never delete user journals or workspace files to make a rollback pass.
