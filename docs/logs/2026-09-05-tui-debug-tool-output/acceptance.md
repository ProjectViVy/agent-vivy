# Acceptance

1. 使用默认配置启动 `vivy-code`，让模型调用一个产生大量结果的工具（例如列出较大的目录）。
2. 已完成工具卡片最多展示 8 行正文，末尾出现 `… N more lines · set tui.debug: true`；聊天页面不再被整份工具结果占满。
3. 在所用配置中加入：

   ```yaml
   tui:
     debug: true
   ```

4. 重新启动后执行同一工具；工具卡片展示完整结果，不出现省略提示。
5. 两种模式下，工具执行结果、会话回放数据和模型行为应保持一致。
