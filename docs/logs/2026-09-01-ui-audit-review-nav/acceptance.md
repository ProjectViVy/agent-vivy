# Acceptance（人如何确认）

1. 打开 `http://127.0.0.1:3015`，左侧主导航（聊天/中控台之后）出现「审批中心」盾形入口；点击进入 `/approvals` 完整页面（非 sheet）。
2. 中文/英文界面下文案分别为「审批中心」/「Approvals」，与聊天 shield 打开的 Review Center 名称一致。
3. 窄屏（390px）下打开导航抽屉同样可见并可点击该入口。
4. 聊天 shield 打开的 review sheet 行为不变（本切片只加导航，不改 review 逻辑）。
