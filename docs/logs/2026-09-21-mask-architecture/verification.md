# Verification evidence

Baseline: `5253f77dfcec6c0d212c2f41d386aeeb7619e1ef`.

- Read issue #43 and both comments through the authenticated GitHub connector.
- Read root/UI rules and applicable plugin/kernel/Eino skills; traced current
  storage migration, admission/edit/fork, action identity, optional assembly,
  presentation registry and browser-local mask paths.
- Inspected pinned Eino v0.9.13 native hooks and agentsmd source; source URLs and
  exact API names are in the design. This is API/source evidence, not executed
  runtime evidence.
- Searched main and the named Channel/issue51/workflow branch trees for embedded
  prompt assets. No existing canonical Markdown assembler was established;
  unpushed work remains unverified.
- `just ci`: attempted; exit 127, `just: command not found`. Repository's required
  product-contract gate is NOT passed. No runtime/browser/Postgres/live-model
  tests were run for this documentation-only delivery.
- Focused documentation validation: `git diff --check` / staged equivalent and
  a local structural check for M1–M9, required sections, balanced fences and
  delivery links. Results recorded at commit preparation; these do not replace CI.

Review addressed atomic ordinary/edit admission, selection/delete races, explicit
legacy vs missing snapshots, checkpoint binding, generation compatibility, safe
error taxonomy, create retry identity, message-byte budget limitations, optional
UI omission and the existing UI instruction conflict. Product evidence remains
pending implementation. There is no claim of deterministic model obedience.

## Planning collection checks

- Re-read supermanagement coordination/Story references and Superpowers writing-plans.
- Verified current Source Catalog binding, checkpoint envelope, Postgres DSN test
  requirement, UI/SDK test scripts and the embedded Playwright configuration.
- Structural check passed for seven plan documents: all local links resolve,
  code fences balance, four Story headers/contract/task sections exist, M1–M9
  coverage markers exist, and no unresolved planning placeholder was found.
- DAG parsed from the index table: unique known IDs, no self edges or cycles;
  computed waves are MASK-1, MASK-2, MASK-3, MASK-4. No redundant transitive edges.
- Self-review corrected Source Catalog ownership, propagated factory metadata
  through frontend_v1.go, unified Service.PromptAssets signature and specified
  how staged UI tests and a separate :3015 browser configuration enter real runners.
- `git diff --check` passed after edits; staged whitespace check is also required
  before this planning commit. These are document checks, not implementation tests.
- No product code changed. Product CI remains unverified: the previously attempted
  required command could not start because `just` is absent; no substitute suite
  is represented as passing. No product runtime, browser or model evaluations run.
