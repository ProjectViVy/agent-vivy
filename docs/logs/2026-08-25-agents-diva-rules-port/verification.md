# Verification

## Commands

| Check | Result |
|---|---|
| `just ci` | pass (2026-08-25, exit 0) |

No Go or UI behavior changed. Gate is still `fmt-check` + `vet` + `test` +
headless compile + `ui-ci`.

## Source of the port

Read-only: agent-diva root `AGENTS.md` (Iteration Protocol, TODOLIST,
Rulebook `iteration-log-required` / smoke / secrets / provider model-id).
Working copy also present at `ui/agent-diva-source/AGENTS.md`.
