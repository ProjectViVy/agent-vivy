# Acceptance

A later agent or human working in this repo should:

1. Treat root `AGENTS.md` as the process guide (still default-scope Vivy,
   still split-pair `:3015`, still air-gap on `data/vivy.db`).
2. For a deliverable, create `docs/logs/YYYY-MM-DD-slug/` with the three
   required markdown files before claiming done.
3. Put leftover bugs in `docs/TODO.md` §0.1 instead of only mentioning them
   in chat.
4. Not look for `LOCK.md`, root `TODOLIST.md`, or a reply-prefix rule —
   those were refused.

This delivery itself is the first use of the ported log protocol.
