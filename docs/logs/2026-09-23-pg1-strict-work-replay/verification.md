# Verification

- Failing-first domain test: paused, blocked, and completed Goals each admitted another round before the fix; cross-session admission was already rejected.
- Failing-first SQLite replay test: a corrupt persisted Goal reference and an unsupported earlier payload version were both returned as valid replay pages before the fix.
- Focused domain and SQLite replay tests: passed after the fix.
- First `just ci`: failed six `internal/modules/masks` tests because the Windows checkout gave embedded Markdown assets CRLF endings; the same failure was confirmed on clean `origin/main`. It also failed `sdk/internal/conformance` because the checked-in internal source digest was stale after the Task 1 code change.
- Local verification adjustment: temporarily converted the four mask prompt files to LF, then `go test ./internal/modules/masks -count=1` passed. No mask file change is in this delivery.
- `go run ./sdk/internal/cmd/source-hash internal ''`: `f390d87d3594468cef3c136f4244e353aa0685daf68d2333cfe15352da3cc2db`. Refreshed the five internal-source entries in `sdk/internal/assembly/conformance_results.json`.
- `go test ./sdk/internal/conformance -run TestCheckedInProviderConformanceMatchesExecutedSuites -count=1`: passed.
- Second `just ci`: passed all steps with the temporary LF mask checkout, including 400 UI tests, Go vet/tests, headless compile, and plugin checks.
- Restored the four temporary mask files and two UI files generated by CI; their diffs are empty. The source digest remains unchanged after restoration.
- `git diff --check`: passed.
- `VIVY_POSTGRES_TEST_DSN` is unset, so no live PostgreSQL conformance result is claimed. PostgreSQL replay uses the same validation call as SQLite and is compiled by the repository gate.

## Fix round 1

- Failing-first tests demonstrated the valid Goal edit rejection, full-history reads on every page, acceptance of mismatched/gapped cursors, and dropped subscription event. All subsequently passed.
- `go test ./internal/domain -run 'TestFoldWork' -count=1 -v`: passed.
- `go test ./internal/storage/sqlite -run 'TestReplayWork|TestBackendConformance/CN-28' -count=1 -v`: passed.
- `go test ./internal/storage/postgres -run 'TestBackendConformance/CN-28' -count=1 -v`: package compiled; live conformance skipped because `VIVY_POSTGRES_TEST_DSN` is unset.
- `go test ./internal/runtime -run 'TestRecoveredGoalRefUsesFoldedWorkReplay|TestForkAndRewindKeepSpentGoalRoundsWithoutChildAuthority' -count=1 -v`: passed.
- `go test ./internal/rpc -run 'TestBuildGoalEditMutationCarriesCurrentReference|TestPlanGetCarriesReplayStateAcrossPages|TestWorkSubscriptionReplaysFromDerivedStateAndContinuesLive' -count=1 -v`: passed.
- `go run ./sdk/internal/cmd/source-hash internal ''`: `71f1876a344c1e79745103bdd19e21865e4c2865acfdef258c193bc1a8dcc1f6`. Five canonical internal-source evidence entries refreshed.
- `go test ./sdk/internal/conformance -run TestCheckedInProviderConformanceMatchesExecutedSuites -count=1 -v`: passed after the evidence refresh. The temporary LF mask prompts and generated UI file were restored; the digest was unchanged.
- Full `just ci` was not rerun in this fix round by explicit direction. Producer conformance, final diff checks, and temporary checkout restoration are recorded in the Task 1 report.
