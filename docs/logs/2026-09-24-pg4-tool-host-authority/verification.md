# Verification

- `go test ./internal/runtime -run '^TestModelEnterPlanWhileGoalArmedReportsGoalArmedWithoutPersistingPlan$' -count=1`: RED before the change. The persisted Goal stayed active, but `tool.finished` contained only `stale goal reference: active Goal must be paused before Plan`, without `goal_armed`. GREEN after the change.
- `go test ./internal/runtime -run '^TestOldGoalRunCannotReportEditedGoalReference$' -count=1`: RED before the admitted-ref check. The old run's forged current revision completed the revised Goal, so the expected revised run never settled. GREEN after the check; the Journal contains no unauthorized completion.
- `go test ./internal/runtime -run '^(TestModelEnterPlanWhileGoalArmedReportsGoalArmedWithoutPersistingPlan|TestOldGoalRunCannotReportEditedGoalReference|TestCreateGoalNeverRequiresHumanApprovalBeforeDurableGoalOrWake)$' -count=1`: PASS.
- `go test -race ./internal/runtime -run '^(TestModelEnterPlanWhileGoalArmedReportsGoalArmedWithoutPersistingPlan|TestOldGoalRunCannotReportEditedGoalReference|TestCreateGoalNeverRequiresHumanApprovalBeforeDurableGoalOrWake)$' -count=1`: PASS.
- `go test -race ./internal/runtime ./internal/rpc ./internal/tools -count=1`: FAIL in existing `TestPlanGoalProbeReportFencesLaterToolInSameBatch`. `internal/rpc` and `internal/tools` passed. Two goroutines wrote the same address at pinned `github.com/cloudwego/eino@v0.9.13/compose/tool_node.go:1253`, through `schema/stream.go:693` and `compose.ToolsNode.Stream` at line 1270. The isolated probe fails the same way at clean HEAD `b6b8ee3` in a temporary detached worktree, which was removed after the check. This predates Task 1; tracked in `docs/TODO.md` as PG4-EINO-TOOLS-RACE.
- The first `just ci` run failed `TestFoldWorkRejectsPlanWhileGoalActive`, which still requires `ErrStaleGoalReference`, and two SDK internal source-evidence checks. The error now wraps both that sentinel and `ErrGoalArmed`; `go test ./internal/domain ./internal/runtime -run '^(TestFoldWorkRejectsPlanWhileGoalActive|TestModelEnterPlanWhileGoalArmedReportsGoalArmedWithoutPersistingPlan)$' -count=1` passes. `go run ./sdk/internal/cmd/source-hash internal ''` returned `fbbdafb5ee2720777553fd225579aac96ba23cc90cb773d94afc60bf74020454`, and the five internal Provider entries in `sdk/internal/assembly/conformance_results.json` now carry it. `TestGenerationFailureMatrixExecutesEveryCase/valid-deterministic-rebuild` also observed the source changing during the first long CI run while the domain fix was applied.
- The next stable-tree `just ci` attempt passed UI typecheck, 400 UI tests, build, i18n, vet, `internal/runtime`, `internal/rpc`, `sdk/internal`, and `sdk/internal/conformance`, but failed because `internal/app` did not exit before its 20-minute package timeout. The collected command output was truncated before the app timeout dump, so the exact blocked test is unknown. Diagnostic-only `go test -v -timeout=5m ./internal/app -count=1` then passed every app test in 56.031s. No `internal/app` source changed in this task.
- One final unchanged-tree `just ci` retry passed completely: UI typecheck and 400 tests, build and i18n, Go vet and full package tests (`internal/app` 37.364s, `sdk/internal` 612.507s, conformance 137.065s), headless compile, and all plugin/face gates. The prior concurrent app timeout remains recorded as a CI stability concern, not a Task 1 failure on the passing final tree.
- Docker and live PostgreSQL checks were intentionally not run under the user's constraint. SQLite-backed scenario tests are unit/integration evidence for the adapter, not PostgreSQL acceptance.
