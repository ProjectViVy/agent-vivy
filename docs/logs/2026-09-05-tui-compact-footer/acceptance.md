# Acceptance

1. Open TUI; the footer should roughly read `shift+tab mode  ctrl+x shortcuts · live` and no longer be crammed with shortcuts.
2. `shift+tab` opens the command palette.
3. `ctrl+x` opens a “Shortcuts” list; `esc` closes it.
4. Original shortcuts such as `ctrl+s` still work.
