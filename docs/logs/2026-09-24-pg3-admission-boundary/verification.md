# Verification

- TDD RED — `go test ./internal/runtime -run '^TestAutomaticPrimaryCannotOvertakeRegisteredHumanIntent$' -count=1 -v`: failed as intended before production edits. The automatic run returned a non-empty RunID and nil error while a human intent was registered, rather than yielding to the human request.
- TDD RED — `go test ./internal/runtime -run '^TestWakeGoalCoalescesSignalsWithoutLosingInFlightEligibility$' -count=1 -v`: failed as intended before production edits because the 64-signal burst blocked behind the in-flight admission candidate. The barrier had durably resumed the Goal while its first candidate still held a stale paused snapshot.
- Focused GREEN — `go test ./internal/runtime -run 'Test(AutomaticPrimaryCannotOvertakeRegisteredHumanIntent|WakeGoalCoalescesSignalsWithoutLosingInFlightEligibility|HumanAdmission|CancelledHumanAdmission)' -count=1 -v`: PASS (4 tests). The coalescing regression asserts the barrier-time single worker/pending bit and exactly one durable admitted round after eligibility changes.
- Focused Plan/LegacyPlan/Goal admission suite — `go test ./internal/runtime -run 'Test(Plan|LegacyPlan|Goal|WorkAdmission|HumanAdmission|AutomaticPrimary|WakeGoal)' -count=1`: PASS.
- Race — `go test -race ./internal/runtime -run 'Test(AutomaticPrimaryCannotOvertakeRegisteredHumanIntent|WakeGoalCoalescesSignalsWithoutLosingInFlightEligibility|HumanAdmission|CancelledHumanAdmission)' -count=1`: PASS, exit 0 (`ok agent-vivy/internal/runtime 6.979s`), no race reports. An earlier race run had no race report but failed an incidental `ReadWork <= 2` count under race scheduling; that assertion was replaced with a direct worker/pending-bit invariant.
- Diff review — `git diff --check`: PASS. Existing line-ending warnings came only from the excluded, pre-existing modified UI generated files.
- Internal source digest — `go run ./sdk/internal/cmd/source-hash internal ''`: `9524238cb143370cdfb593ede65d68469c5b73836d8240a22054d7030a7ff410`; refreshed the five matching entries in `sdk/internal/assembly/conformance_results.json`.
- SDK conformance digest test — `go test ./sdk/internal/conformance -run '^TestCheckedInProviderConformanceMatchesExecutedSuites$' -count=1`: PASS, exit 0 (`ok agent-vivy/sdk/internal/conformance 187.701s`).
- Full repository `just ci` — PowerShell prepended `C:\Program Files\Go\bin` to PATH, then ran `just ci 2>&1 | Tee-Object -FilePath $logPath`: PASS, exit 0. Live log: `%TEMP%\issue47-pg3-task1-just-ci-20260924.log` (`C:\Users\Administrator\AppData\Local\Temp\1\issue47-pg3-task1-just-ci-20260924.log`). UI gate passed (49 files / 400 tests, typecheck/build/i18n); `go vet ./...` passed; `go test -timeout 20m ./...` passed including `internal/runtime` 451.708s, `internal/rpc` 300.091s, `sdk/internal` 899.585s, and `sdk/internal/conformance` 300.140s; headless compile passed; plugin/face vet+tests passed. The Vite bundle printed its existing advisory that the generated JS chunk exceeds 500 KB; it was not a failure.

Call-site review: `turn/start` and session edit set `HumanAdmission`; the channel callback and authenticated Action `StartRun` do likewise after source/identity validation; the headless user prompt is human-originated. Cron passes default options and is automatic; Goal round admission passes through the same session gate and checks pending intent immediately at its atomic commit boundary. The startup gate spans durable startup preparation and activation only: `runWithAdmissionGate` launches `drive` and returns, then deferred unlock runs before the asynchronous model call. No gate is held through model execution or drain.

Pinned Eino remains v0.9.13 with existing EinoExt versions unchanged. Admission arbitration is Vivy Service/Journal behavior; the normal existing Eino ADK runner path is unchanged. No PostgreSQL service/DSN or downstream browser/model acceptance was supplied or claimed.
