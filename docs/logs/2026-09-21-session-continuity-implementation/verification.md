# Session Continuity implementation verification

## T0 baseline refresh — 2026-09-22

This log records observed planning evidence only. No Session Continuity product code, migration, runtime, RPC, UI, recipe, generated assembly, issue, or PR state was changed by T0.

### Observed baseline facts

- Code baseline: `a8d361b0244a1c40be513622bbdaebb5c9d40014`.
- PR #45 is merged through `680ef78`; its centralization implementation is `760ac1c`.
- Central migration owner: `internal/storage/migrations`, with paired embedded SQLite/PostgreSQL SQL and `manifest.go`/`runner.go`.
- Observed migration files: `internal/storage/migrations/sqlite/023_workspace_path.sql` and `internal/storage/migrations/postgres/023_workspace_path.sql`; `024` is the next available logical number. T0 did not reserve or create it.
- `origin/feat/issue-47-goal-plan-foundation` is identical to current main and contains no `session_work_events` implementation or migration; Issue #47 remains design-only.

### Message-writer audit scope

Command:

```sh
rg -n 'INSERT.*messages|AppendMessage|AppendMessageIfAbsent' internal/storage
```

Observed implementation writers: `internal/storage/sqlite/messages.go`, `internal/storage/postgres/messages.go`, `internal/storage/sqlite/history_mutations.go`, and `internal/storage/postgres/history_mutations.go`. The audit also returned the shared contract plus conformance, upgrade, registry, SQLite, and file-context tests. The planned future position allocation must cover ordinary `AppendMessage`, deterministic `AppendMessageIfAbsent`, history mutations, Journal projections, and fork writers; no product code was changed.

### Available checks

| Command | Result |
| --- | --- |
| `node --test scripts/check-plugin-v1-fixtures.test.mjs` | PASS — 5 tests passed, 0 failed. |
| `node scripts/check-plugin-v1-fixtures.mjs` | PASS — fixture corpus reported 13 cases (2 accept, 11 reject). |
| `node --test scripts/check-i18n-cross-face.test.js` | PASS — 8 tests passed, 0 failed. |

### Unverified checks and environment skips

| Command | Result |
| --- | --- |
| `just ci` | UNVERIFIED — exit 127: `/bin/bash: just: command not found`. This is not a passing CI result. |

`node` and `pnpm` are available. `go`, `just`, PowerShell (`pwsh`/`powershell`), Docker, PostgreSQL client (`psql`), Chromium, and Playwright are unavailable; `ui/node_modules` is absent. Accordingly, Go tests, `just ci`, PostgreSQL checks, HTTP/UI browser smoke, and Playwright/browser-cache checks are unverified, not passes.

### T0 conclusion

T0 refreshes the SC-D4/SC-P5 documentation and records evidence only. T2 and T4 are Planned because the centralized migration owner is present, but no product Story is Ready: each still requires implemented and accepted predecessor evidence.

## Phase 0 toolchain burn-down — 2026-09-23

`go` (go1.26.4 windows/amd64) and `just` (1.46.0) became available on the host, lifting the exit-127 block recorded above. This section re-runs the T1–T3 focused verification debt and records observed results. PostgreSQL remains unavailable (`VIVY_POSTGRES_TEST_DSN` unset): every Postgres-gated case stays SKIP — not a pass. `pnpm`/`node` are available; `ui/node_modules` is installed by the ci `ui-ci` step.

### Findings and fix-forward classification

The first-ever execution of the T1–T3 test surface exposed four never-run defects. All four are stale artifacts of stories accepted without execution evidence; the production contracts themselves are intact, so each was fix-forwarded per the Phase 0 rule and the owning story rows stay Accepted with this annotation:

| Finding | Class | Fix commit |
| --- | --- | --- |
| `TestEventVocabulary` golden 38, actual 40: T1 intentionally added `context.reference_attached` and `deliverables.presented` but never updated the golden | Stale T1 test golden | `466c7cfa` |
| `TestContinuityCanonicalHistoryScopeHash` golden `6d9aef6d…` was a placeholder; the deterministic implementation computes `a88372eb…`, independently confirmed via `sha256sum` of the canonical JSON | Stale T1 test golden | `466c7cfa` |
| `TestMessagesPersistImageAttachments` appended the reborn message before creating session `s-att2`; T2's fail-closed position allocation requires the session row first. Reordered the fixture; assertion intent unchanged | Stale pre-T2 fixture vs legitimate T2 contract | `d8a7b873` |
| `internal/runtime` test binary did not compile: T3's `history_service.go` redeclared `truncateUTF8` (existing: `tooladapter.go`) and `containsString` (existing: `search_backend_test.go`). Additionally `TestHistoryTraceRejectsMissingTrustedSession` failed: the `query == nil` unavailable disclosure preceded the missing-authority check in Search/Read/Trace, letting unauthorized callers probe capability state | T3 implementation defects | `28b01b67` |
| 14 tracked `.go` files from T1–T3 commits were never gofmt-ed (fmt-check had never observed them) | Formatting debt | `b5c1d65a` (plus the three commits above) |

Fixes reused existing helpers (`takePrefixUTF8`, `slices.Contains`) per single-source-of-truth; the duplicate `truncateUTF8` in T3 also returned the full text on a zero budget, while the retained `takePrefixUTF8` is fail-closed.

### Observed results after fixes

| Command | Result |
| --- | --- |
| `go test ./internal/domain/... -count=1` | PASS |
| `go test ./internal/storage/... -count=1` | PASS (sqlite 45.0s; postgres package PASS via DSN-gated SKIP — unverified, not a pass) |
| `go test ./internal/runtime ./internal/tools ./internal/rpc -run 'TestHistory' -count=1` | PASS (runtime 7.0s, tools 0.1s, rpc 0.2s) |
| `gofmt -l` over all tracked `*.go` | clean (0 files) |
| `just ci` | PENDING — recorded in the addendum below |

### Phase 0 addendum

Full-suite burn-down (2026-09-23, after `go`/`just` became available):

| Command | Result |
| --- | --- |
| `go test ./internal/runtime ./internal/app ./internal/rpc -count=1` | PASS after fixture fix `29908309` (73 stale fixtures across 21 test files called `Run`/`AppendMessage`/`CreateRun` without the session row that T2's fail-closed position allocation requires; test-only change, no assertions modified). Before the fix: 66 runtime + 6 app + 1 rpc FAIL, all `storage: not found`. |
| `go test ./internal/runtime ./internal/rpc ./internal/app ./internal/tools ./internal/storage/... -count=1` | PASS after `d3f4878b` (runtime 82.2s, rpc 51.6s, app 30.7s, tools 0.6s, storage/sqlite 51.9s, storage/migrations 1.4s, storage/postgres PASS via DSN-gated SKIP — unverified, not a pass). |
| `go test ./sdk/tui/live ./sdk/tui/stream -count=1` | PASS after `d3f4878b`. |
| `cd faces/headless && go test ./... -count=1` | PASS (separate module) after `a320f55f`. |
| `cd ui && pnpm vitest run src/components/chat/ChatView.test.tsx src/lib/run-rows.test.ts` | PASS — 14/14 after `d3f4878b`. |
| `gofmt -l internal sdk` | clean (0 files). |
| `go vet` (runtime, sdk/tui/live, app, rpc, provider) | clean. |
| `just ci` | PASS on `41e67904` (successor lane, Linux + pwsh 7.6.6, go1.26.4, just 1.58.0, node 24.21.0, pnpm 11.19.0): fmt-check clean; ui-ci green (tsc, vitest 392/392 across 48 files, vite build, i18n completeness + cross-face checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok including internal/runtime 22.7s, internal/rpc 8.3s, sdk/internal 220.9s, sdk/internal/conformance 65.3s; headless-compile ok; plugin-ci all `plugins/*` and `faces/*` modules ok. Two stale conformance digests surfaced and were fixed first in `cd30a886`/`41e67904` (internal-rooted suites now record `478b59c8`; `vivy/headless` records the `948c24ec` self-referential fixed point). ci-generated `ui/src/generated/assembly.ts` drift was `git restore`d, not committed. |

Contract change landed during Phase 0/T3 closure (user-directed): `a320f55f` removed the legacy v1 `model.completed` read path everywhere (v2 hash-commit is the only accepted shape; v0/v1 fail closed). `d3f4878b` fixed the two re-review blockers: reconcile skips pre-v2 legacy runs via the `errUnsupportedCompletedVersion` sentinel so old journals stay readable (run-completion path still fails closed), and the remaining v1-shaped fixtures/readers in `sdk/tui/live` and `ui/` were moved to v2.

### T4 — Atomic task admission and operation receipts (commit `e337ac5d`)

Scope delivered: `storage.ContinuityStore` (`CommitContinuityRun` / `CommitContinuityOperation` / `FindContinuityReceipt`) on both backends; migration `025_continuity_receipts` (PK `(session_id,operation,request_id)`, rebuildable index into the committed events); sorted-ID session locking (SQLite serialized writer tx, Postgres `SELECT ... FOR UPDATE`); source expectations pinned to view-truncation revision + session existence; receipt-first dedup with `NewlyCommitted` replay; `ErrSourceChanged` / `ErrCommitUncertain` distinguishing definite rollback from uncertain outcome; runtime `RunOptions.Continuity` admission path with `AdmissionWorkspaceAllocator` (new-private rollback, selected/local never removed, uncertain-commit preserve); `run.started` payload now carries the accepted `HistoryScope`; `runPersistence` seam widened to return all committed startup events (EditSession adapted, ordinary path unchanged); session delete removes receipts in the same transaction.

| Command | Result |
| --- | --- |
| `go test ./internal/storage/... ./internal/runtime -run 'TestContinuityAtomic|TestContinuityRetry' -count=1` | PASS (sqlite 0.075s, runtime 0.127s; postgres PASS via DSN-gated SKIP — unverified, not a pass) |
| `go test ./internal/storage/... -count=1` | PASS (sqlite 6.5s incl. `TestContinuityExpectations` sorted-lock/rewind/delete rechecks; migrations count tests updated 24→25) |
| `go test ./internal/runtime -count=1` | PASS (23.0s — ordinary-turn regression covered by the unchanged suite) |
| `git diff --check` | clean |
| `just ci` | PASS on `e337ac5d`: fmt-check clean; ui-ci green (tsc, vitest 392/392, vite build, i18n checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/runtime 24.1s, sdk/internal 220.2s, sdk/internal/conformance 67.1s after digest refresh `21789b86`); headless-compile ok; plugin-ci all `plugins/*` and `faces/*` ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |

Test-map notes: fault-injection atomicity uses the admission/mutation `TestFaultHook` per insertion step (user-row, run, each event, receipt) — reopen asserts all-or-zero; duplicate admission asserts `NewlyCommitted=false`, single run/message/startup events; identical replay returns the original `RunID` including after terminal completion; changed payload under the same request_id → `ErrConflict`; operation on terminal run → `ErrRunClosed` except identical replay; terminal-vs-operation race resolves via receipt lookup before the terminal check; cancel-before-commit aborts via statement ctx and rolls the new private dir back; `ErrCommitUncertain` preserves the dir; duplicate retry never re-allocates (receipt pre-check precedes workspace allocation). Crash between commit and engine scheduling resolves through the same receipt pre-check on retry — the committed run is returned to the caller and the existing startup recovery path owns its interrupted state; recovery never executes model/shell as a receipt repair. Incidentals fixed inside the same commit: secret-audit literal collision (`task-admission` → `task admission`) and internal source digest refresh.

### T5 — Explicit reference preview and attachment (commit `5019b4d6`)

Scope delivered: `runtime.ReferenceService` (`Preview` / `Attach` / `AttachForAdmission` / `Lookup`) over the T3 `HistoryService`; `tools.ReferenceOperations` with the `reference_context` effectful tool (`Readonly:false`, strict args schema); `reference/preview` RPC under operator authority with spoof-field rejection and strict body decode; `turn/start` `request_id`+`references`+`history_scope` decoding into `domain.ContinuityInput`; admission-time snapshots committed atomically as `context.reference_attached` startup events through T4 `CommitContinuityRun` (source unavailable fails the whole submission and rolls the draft back); model attaches go through `CommitContinuityOperation` with the stable tool_call id as receipt `request_id` (duplicate call replays the original reference); digest semantics — `expected_digest` non-empty must equal the canonical selection digest else `ErrSourceChanged`, empty attaches current; limits `ReferencesPerTask`/`ReferenceBytes`/`ReferencesTotalBytes` enforced at attach; reference-id reads via `HistoryService.SetReferenceLookup` return the destination-owned captured copy only; composition root wired in `internal/app`; `compose.GetToolCallID` propagated into tool ctx via `tools.WithToolCallID`.

| Command | Result |
| --- | --- |
| `go test ./internal/runtime ./internal/tools ./internal/rpc -run 'TestReference' -count=1` | PASS (runtime 0.97s, tools 0.01s, rpc 0.11s — 20 reference tests) |
| `go test ./internal/runtime -count=1` | PASS (24.99s under `just ci`) |
| `git diff --check` | clean |
| `just ci` | PASS on `5019b4d6`: fmt-check clean; ui-ci green (tsc, vitest 392/392, vite build, i18n checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/runtime 24.99s, sdk/internal 220.4s, sdk/internal/conformance 64.4s after digest refresh `fc095ee2`); headless-compile ok; plugin-ci all `plugins/*` and `faces/*` ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |

Test-map notes: `newReferenceFixture` shares one sqlite backend across `HistoryService` + `ReferenceService` + `Service`; excerpt attach asserts the admitted scope resolves to the destination session alone (excerpt never broadens HistoryScope), `SourceSessionID`/`Origin`/`Digest`/`SourceWorkspace` pinned; adjacent-session attach under a run scoped to A → forbidden, zero events committed; digest conflict → `ErrSourceChanged` and no run row persists; model attach without call identity → forbidden; invalid digest encoding → invalid_argument; per-task and per-reference budgets refuse without silent truncation; source deletion fails the submission recoverably; reference-id read returns the captured copy and the source session never sees it. No local PostgreSQL in this environment — DSN-gated tests SKIP (unverified, not a pass).

### T6 — Feed projection and historical lifecycle (commit `92f6b8f9`)

Scope delivered: `contexthost.ResolvedSnapshotSource` (memory-only, `TreatmentCompetitive`, defensive copies, cannot search history); `runtime.ProjectReferenceContext(ctx, refs, remainingBytes)` projecting each committed snapshot as one explicit `[imported reference: id | origin | source | digest | captured]` user-data block with per-item author/text lines, reserving marker bytes so tight budgets emit `state=elided_budget`/`state=unavailable` markers instead of silent drops; `projectable` validation (id/digest/items/utf8) with `unavailable` for corrupt payloads — Lookup surfaces decode failures as an explicit error, never an empty substitute. Feed wiring: `Service.sessionAttachedReferences` → `buildRunContextWithReferences` → `projectFeedWithContextBudget` → `userFeedMessageWithContextBudget` appends imported parts after file/attachment parts inside the shared `contextProjectionBudget`; only startup-region events (run.started + contiguous `context.reference_attached`) count — a same-run model attach rides its tool result and is never re-injected. `reference/get` RPC returns `domain.ReferenceView{reference, source_status, feed_status}` — destination-owned copy, live source session check (`ok`/`source_unavailable`), and effective-view feed status (`included`/`hidden`/`detached`/`unknown`) via `SetViewStores`. Lifecycle: `CompactSession` folds per-run `[references: ids]` lines into the model summary plus a deterministic `Folded references:` manifest and `payloadContextCompacted.ReferenceIDs`; `ForkSession` copies only the included-prefix runs' startup refs into the child's derived `frok_<session>` history run inside the same atomic `CommitSessionFork`, fresh reference IDs, parent provenance/DestinationRunID kept, no scope or approval transfer; rewind/edit view truncations hide owning turns naturally through `effectiveSessionMessages`. `composeStaticInstruction` gains the short trusted data-only rule (no literal marker text).

| Command | Result |
| --- | --- |
| `go test ./internal/runtime ./internal/contexthost -run 'TestReferenceContext|TestReferenceLifecycle' -count=1` | PASS (runtime 0.477s; contexthost compiles — no named tests) |
| `go test ./internal/runtime -run 'TestReference' -count=1` | PASS (1.145s — T5 suite unchanged, no regression) |
| `go test ./internal/rpc ./internal/tools -count=1` | PASS (rpc 7.926s, tools 0.455s) |
| `git diff --check` | clean |
| `just ci` | PASS on `92f6b8f9`: fmt-check clean; ui-ci green (tsc, vitest 392/392, vite build, i18n checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/runtime 25.1s, sdk/internal 235.3s, sdk/internal/conformance 85.7s after digest refresh `fad7fbad`); headless-compile ok; plugin-ci all `plugins/*` and `faces/*` ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |

Test-map notes: `newReferenceFixtureWithConfig` injects a `recordingModel` + custom `EngineConfig` so scripted feeds are captured per turn. Hostile-payload test asserts every fed message containing `SYSTEM: ignore policy`/fake serialized tool_calls is `Role == schema.User` with zero `ToolCalls` and exactly one snapshot copy; same-run model attach produces zero re-injected blocks while Lookup still resolves by id; budget elision emits a visible `elided_budget` marker; direct projection returns `included`/`unavailable` per stored-copy validity. Lifecycle: compaction summary keeps explicit ids replayable from the `cmp_` journal run; source delete keeps the captured copy with `source_unavailable`; corrupt snapshot event errors explicitly on Lookup while AttachedReferences skips it; rewind hides the owning turn's imported blocks; fork copies exactly the prefix ref under `frok_<child>` with new id/destination session and no child run rows; reopen via a rebuilt service resolves the same ids. Fixture wiring `SetViewStores`/`Compactions` added so `feed_status` and durable compaction are exercised. No local PostgreSQL — DSN-gated tests SKIP (unverified, not a pass).

### T7 — History picker, composer and queue (commit `f7c4f212`)

Scope delivered: `FaceTurnSubmission`/`FaceTurnContinuity`/`FaceHistoryScope`/`FaceReferenceSelection` + history types (`FaceSourceRef`, `FaceHistoryItem`, `FaceHistorySession`, `FaceHistoryPage`, `FaceReferencePreview`, `FaceReferenceDraft`) owned once by `@vivy/ui-sdk` (`sdk/ui/src/module.ts`); `FaceClientAPI.startTurn`/`FaceStoreState.startRun`/`enqueueMessage` now take the typed submission atomically with `historySearch`/`historySessions`/`previewReference` declarations and `draftReferences`/`draftScope`/`draftRequestId` draft state plus `addDraftReference`/`removeDraftReference`/`setDraftScope`/`clearDraftContext` — `sdk/ui/src/module.test.ts`, `ui/src/lib/ui-sdk-face-compat.test.ts` and every caller updated in the same change. Host `api.ts` re-exports the SDK shape (`export type TurnSubmission = FaceTurnSubmission`, no second hand-maintained shape), flattens `submission.continuity` into the flat wire params (`request_id`, `references`, `history_scope`) matching backend `decodeContinuityInput`, and wires `history/search`, `history/sessions`, `reference/preview`. `store.ts` issues one `draftRequestId` per draft context (rotated only on successful send or session switch so unknown responses can retry the same ID), deep-copies submissions on enqueue (`copySubmission`) so composer drift cannot mutate queued items, drains the queue from a shared `drainQueue()` on run completion and on head removal (failed send re-inserts the retained head, no reorder, no digest refresh), and recomputes `draftScope` on chip removal. `HistoryPicker` renders the §13 layout — ≥768px `Dialog` `min(960px, vw-32px) × ≤80dvh` with a 280px session column (metadata only), <768px full-height bottom `Sheet` with Back that preserves selections; record search is submit-only with a generation counter discarding stale session/query responses; multi-select with count+byte estimate; over-limit selection stays selected with the narrowing hint and Attach disabled; `source_status !== 'ok'` disables Attach; "allow further reading" unchecked by default adds the source session to `history_scope` only when checked; Preview precedes Attach. `ContextReferenceChip` shows session/count/kinds/capture time with view/remove. `ChatInput` gains the attach-history button + chip row + picker; `ChatView` composes `TurnSubmission` from draft state and calls `clearDraftContext()` only after successful submit/queue so failed sends retain text/images/references. `scripts/i18n-cross-face-contract.json` gained the 31 new web keys; en/zh catalogs updated.

| Command | Result |
| --- | --- |
| `cd ui && pnpm test src/lib/store.test.ts src/components/chat/HistoryPicker.test.tsx src/components/chat/ContextReferenceChip.test.tsx` | PASS (47/47) |
| `cd ui && pnpm typecheck` | PASS |
| `cd sdk/ui && vitest run` (local binary; `pnpm test` blocked by deps-status policy) | PASS (28/28 incl. new typed-submission chain test) |
| `cd sdk/ui && tsc --noEmit` | PASS (fixture gaps `catalog`/`chooseWorkspace`/`browseWorkspace`/`setSessionWorkspace`/`created_at` fixed in `module.test.ts`) |
| `node scripts/check-i18n-cross-face.js` | PASS (13 shared units; new keys classified in `faceSpecific.webKeys`) |
| `git diff --check` | clean |
| `just ci` | PASS on `f7c4f212` content: fmt-check clean; ui-ci green (tsc, vitest 404/404, vite build, both i18n checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/runtime 23.4s, sdk/internal 225.4s, sdk/internal/conformance 68.4s); headless-compile ok; plugin-ci all `plugins/*` and `faces/*` ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |
| UI smoke at `http://127.0.0.1:3015` (split pair `just run` + `cd ui && pnpm dev`) | PASS in real Chrome: dialog opens from attach-history icon; session column lists metadata only, search filters; records load only after session choice; kind chips + checkboxes + `1 selected · ~25 B` footer; further-reading unchecked; Preview shows `1 records · 246 B · ok` and gates Attach; chip `History: sess_… · 1 records · message · …` renders and removes; send with chip commits `context.reference_attached` (origin `user_selection`, source session + selected item, digest) at admission plus a `continuity_receipts` row carrying the client `request_id`, and the draft clears; <768px renders bottom Sheet with Back; zh locale fully translated. No LLM provider key on this box — the run then fails at `model.request` (expected environment limit, not a defect); submission provenance verified from the journal. Cosmetic stale-items nit found in smoke (mobile Back did not clear `items`) fixed before acceptance. |

Test-map notes: store tests pin continuity through queue → `startTurn` (`references`/`history_scope.session_ids`/`request_id` equal the queued submission), stale-head retention on send failure, resume-after-head-removal via `removeQueuedMessage` → `drainQueue`, draft lifecycle/scope recompute and session-switch reset (including a mutation-drift guard on `copySubmission`). Picker tests cover metadata-only open, submit-only search + previewed attach, opt-in further-reading, over-limit narrowing with selection retained + Attach disabled, stale-response discard on session switch, cancel, and the <768px Sheet + Back (window.innerWidth stub). Smoke caveat above: end-to-end model output of referenced history requires a provider key; admission-level provenance is journal-verified. No local PostgreSQL — DSN-gated tests SKIP (unverified, not a pass).


## T8 — Reference details and model history timeline

Scope delivered: `foldContinuityRows` in `ui/src/lib/run-rows.ts` is the typed continuity-event projection merged into `foldRunEvents` — `context.reference_attached` events become `RunRowContextReference` rows deduped by `run_id:seq` event key AND `reference.id`, so live+replay unions and identical fixtures never produce a second card, and the corresponding tool result stays a normal tool row (never a card). Malformed/missing-payload/empty-id events are skipped. `ui/src/components/chat/ReferenceDetail.tsx` is the neutral imported-data card: header shows title + `source_session_id` + record count + kinds + origin badge; expanding lazily calls `loadReferenceView` → `reference/get` and renders `source_status`/`feed_status` as localized badges separately from the committed excerpt (plain-text items, redacted italic). The "View source" button issues `history/read` with `{selection: {session_id, refs}}` and renders live source rows in place — it never changes the active destination session and is disabled only when the live status is unavailable/denied; the saved copy stays readable (fetch failure stores `null`, no fabricated status). Escape collapses and returns focus to the header toggle. `internal/rpc/references.go` `referenceGet` now checks session existence via `deps.Sessions.GetSession` → `CodeNotFound` before the service call. History tools (`history_search`/`history_read`/`history_trace`/`reference_preview`) render under a `History` title variant with expandable args/results. `RunInspector` run tab lists `foldContinuityRows` references with count. SDK Face contract gained `FaceContextReference`, `FaceReferenceView`, `FaceHistoryReadRequest`, `referenceGet`, `historyRead`, `referenceViews`, `loadReferenceView` — mirrored in `module.test.ts` fixtures atomically (bidirectional `never` compat test). `scripts/i18n-cross-face-contract.json` gained 31 web keys; en/zh catalogs updated.

| Command | Result |
| --- | --- |
| `cd ui && pnpm test src/lib/run-rows.test.ts src/components/chat/ReferenceDetail.test.tsx src/components/chat/ChatView.test.tsx` | PASS (per story: 15 + 4 + 4; with store.test.ts 64/64) |
| `cd ui && pnpm test` | PASS (416/416, 51 files) |
| `cd ui && pnpm typecheck` | PASS |
| `cd sdk/ui && ./node_modules/.bin/vitest run` + `tsc --noEmit` | PASS (`pnpm` still blocked by deps-status policy) |
| `go test ./internal/rpc -run 'TestReference'` | PASS |
| `git diff --check` | clean |
| `just ci` | PASS on `15096e78`: fmt-check clean; ui-ci green (tsc, vitest 416/416, vite build, both i18n checks); `go vet` clean; `go test -timeout 20m ./...` all ok (incl. conformance digest refreshed to `e8aec94d…` for the references.go change — fixed point verified); headless-compile ok; plugin-ci all ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |
| UI smoke at `http://127.0.0.1:3015` (split pair, real Chrome, dev DB persisted from T7) | PASS on committed `run_419faabb8669ff07` (replay path after reload): exactly ONE collapsed `Imported reference · sess_07e41bace258d003 · 1 records · Messages` card at the attach position; expand → `Source ok`/`Feed included` badges, Saved excerpt "history source seed alpha", collapsed details, enabled View source → live source rows rendered in place and the sidebar highlight stayed on the destination session. UNTESTED (cut early on user request): Escape-collapse/refocus (unit-tested), multi-card dedup at runtime (unit-tested), history tool rows + RunInspector refs section (code wired). |

Test-map notes: `run-rows.test.ts` pins fold dedup (2 refs → 1 row), position, no double card from `tool.finished`, and replay/live union; `ReferenceDetail.test.tsx` covers expand → excerpt + localized statuses ('elided (budget)'), unavailable → jump disabled + copy readable, `history/read` source jump without session switch, Escape collapse + focus return; `store.test.ts` covers per-id view caching, failure → `null`, session-switch clearing. rpc tests cover authority dispatch, spoofed/missing-field rejection and unknown-session `CodeNotFound`, plus hidden-without-service. No local PostgreSQL — DSN-gated tests SKIP (unverified, not a pass).


## T9 — Safe file presentation and verified transfers

Scope delivered: `internal/runtime/workspace_files.go` gained the shared `SensitiveWorkspacePath` classifier (moved verbatim from rpc), and `internal/rpc/project_context.go` + `internal/rpc/attachments.go` now delegate to it — the safe-open code is runtime-owned inward of rpc callers. `internal/runtime/deliverable_service.go` adds `DeliverableService`: `Present` (tool-context identity → request validate → dup-path reject → `manager.Existing(runID)` trusted workspace → per-file fingerprint → honest ok/partial/failed set → `deliverables.presented` committed through the T4 receipt transaction, replay decodes the original set), `List` (keyset cursor `millis36:id` over replayed events), `Get`, `Read` (digest conflict → conflict status; otherwise open-or-continue transfer and serve one page), `CloseTransfer`, `CloseSessionTransfers`. `internal/runtime/deliverable_transfer.go` adds the os.Root-bound `openWorkspaceFileSecure` (pre-Lstat rejects FIFO/socket before any open; EvalSymlinks preflight rejects escapes and link swaps; post-read `recheckIdentity` catches mid-read replacement) plus `transferManager`: per-owner single slot, process-scoped opaque `xfr_` handle, 60s idle TTL on an injectable clock, 256KiB page bound, dedicated scratch purged at startup only. First read hashes the full bounded file into a scratch snapshot — a mismatch deletes it and reports `changed`; subsequent chunks require live owner + matching item/digest and a sequential (or last-window replay) offset. `service.go` gained `ServiceDeps.Deliverables` (SessionTransferCloser) called from `DeleteSession`.

| Command | Result |
| --- | --- |
| `go test ./internal/runtime -run 'TestDeliverable|TestWorkspace' -count=1` | PASS (14 tests: present ok/partial/failed sets, idempotent replay, dup-path reject, escape/sensitive/link/FIFO refusal, verified snapshot serve + sequential offsets + last-chunk replay, changed/missing/linked-file table, missing workspace, owner+digest enforcement, transfer replacement + idle expiry, close + session-delete hook, paging + get scoping, media-type sniff + startup scratch purge, mid-read swap detect, unwritable-scratch failure) |
| `go test ./internal/rpc -count=1` | PASS (delegation to runtime.SensitiveWorkspacePath keeps all caller behaviour) |
| `git diff --check` | clean |
| `just ci` | PASS on `f9d8f4b7` content: fmt-check clean; ui-ci green (tsc, vitest 416/416, vite build, both i18n checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/runtime 26.4s, sdk/internal 217.0s, conformance 64.7s — internal source digest refreshed to `27b6b2b8…`, fixed point verified); headless-compile ok; plugin-ci all `plugins/*` + `faces/*` ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |

Test-map notes: fixtures mirror `newReferenceFixture` — real sqlite backend + WorkspaceManager over `t.TempDir()` with manually created per-run dirs; fake clock injected via `svc.now` (the transferManager reads `s.now()` lazily so expiry tests are deterministic). Platform fixtures: `deliverable_transfer_unix_test.go` provides `syscall.Mkfifo` FIFO creation (windows twin is a skip stub); symlink replacement is exercised on the READ path and maps to forbidden — never followed. Windows junction boundary is documented behavior of the lexical-real==canonical-real check. No local PostgreSQL — DSN-gated tests SKIP (unverified, not a pass).


## T10 — Presentation tool, RPC and durable discovery

Scope delivered: `internal/tools/present.go` registers `present_files` as an effectful metadata tool — schema declares `files[]{path,description}` + `title` with `additionalProperties:false` at every level, so url/upload/workspace_id/actor and any unknown key fail strict decode before the service is reached; the tool result carries `set_id` + status + items + failures. `internal/rpc/deliverables.go` adds `deliverables/list|get|read|close`, all through the shared `historyAuthorityContext` (session existence + trusted `WithSessionID` identity — no caller body can supply authority) and `decodeStrictJSON`/`rejectHistorySpoofFields`; `DeliverableError` maps to the common envelope as `InvalidParams` carrying the stable `status: reason` pair. `DeliverySetPage` moved to `internal/domain` so tools/rpc share the envelope without importing runtime. `DeliverableService.SetViewStores(messages, truncations)` now drops sets whose committing turn a rewind/edit marker hides — runs without an owning user message stay listed (cannot be proven hidden). `HistoryService.Search` enables `artifact_id`: only `deliverables.presented` events whose committed payload contains the set or item id match (provenance, never filename text); `task_id` stays `unsupported_filter`; the event projects to a `presented N file(s) · set <id>` history item (`historyEventAllowed` + payload v1). App wiring: `NewDeliverableService` over `dataRoot/transfers` scratch (created 0700, startup-purged only), `WithDeliverables` in the builtin registry rebuild, `ServiceDeps.Deliverables` (nil-safe SessionTransferCloser on `DeleteSession`), `ControlDeps.Deliverables` gated capability block — nil service → no registration, no capability, no method.

| Command | Result |
| --- | --- |
| `go test ./internal/tools ./internal/rpc ./internal/runtime -run 'TestPresent|TestDeliverable' -count=1` | PASS (tools 6 incl. spec/forged-field/registry checks; rpc 6 incl. capability advertise+hide, list/get/close roundtrip, net.Pipe JSONL transport read, stable changed-reason envelope, spoof/unknown/foreign reject; runtime 18 incl. all T9 tests + scripted tool commit → restart List recovers identical set+digest, plan-profile deny commits nothing (`bypassedPolicy` guarded), rewound-turn exclusion from List AND Get, artifact search by set/item id with unrelated + out-of-scope ids disclosing nothing) |
| `git diff --check` | clean |
| `just ci` | PASS on `89d71bde` content: fmt-check clean; ui-ci green (tsc, vitest 416/416, vite build, both i18n checks); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/runtime 26.5s, internal/tools 0.45s, internal/rpc 7.5s, sdk/internal 210.6s, conformance 63.0s — digest refreshed to `00d1b7b4…`, fixed point verified); headless-compile ok; plugin-ci all ok. ci-generated `ui/src/generated/assembly.ts` drift `git restore`d. |

Test-map notes: the "scripted model" check invokes `NewPresentFiles` through the real registry path; `bypassedPolicy` is proven by `newToolAdapter` under `PolicyProfilePlan` refusing before commit (no event lands). Receipt replay: a second `Present` under the same tool-call identity returns the original committed set (T9 coverage, unchanged). Event-size ceiling: request validation caps at PresentPaths=20 × bounded description, so a committed set event stays far under the journal payload bound — structural bound, not a separate test. No local PostgreSQL — DSN-gated tests SKIP (unverified, not a pass).

## T11 — Additive delivery groups and GUI actions

Scope delivered: `ui/src/lib/deliverable-download.ts` owns the transfer lifecycle — `downloadDeliverable` sequences digest-bound `deliverables/read` chunks (lazy-RPC seam so store spies intercept call time), captures the server-issued `transfer_id` before any validation so a mismatched or aborted transfer is still closed via `deliverables/close`, verifies frame offset/size and assembled sha256, then emits one object URL + `<a download>` and revokes it; `checkDeliverable` probes ≤4KiB, `readDeliveryPreview` decodes ≤64KiB UTF-8, `isTextPreviewable` keeps text/* minus text/html plus image/svg+xml and binary types out of the preview path (HTML/SVG only ever download as attachments, never execute in app origin). `run-rows.ts` gains a `deliverables` row kind folded from committed `deliverables.presented` events (deduped by run+seq key and set.id, additive chronological order — no success card before the commit lands). `DeliverableCard` renders the immutable set: collapsible header (title + `{{count}} files delivered` + failure count + status badge), per-item path/size/media_type, `role="status" aria-live` explanatory states (unchecked/checking/verified/changed/missing/unavailable/downloading/downloaded), Verify/Preview/Download-or-Cancel buttons with per-name `aria-label`s, a `<ul>` failure list, and a `<pre data-delivery-preview>` plain-text preview. `FilesPanel` renders a Deliveries section above the file list (and in the no-run branch) reading the same store data; each row focuses its original transcript group via `scrollIntoView` — separate from the modified-file list. `store.ts` adds `deliverySets`/`deliverySetsPhase`/`deliveryItemStates` plus load/check/preview/download/cancel actions; `deliveryDownloads` AbortController map enforces one active transfer per item, cancel → 'unchecked' retryable, typed `DeliverableTransferError.reason` → state; `loadDeliverySets` dedupes sets across cursor pages preserving order. `api.ts` adds `deliverables/list|get|read|close`; `sdk/ui` Face mirrors every new API operation + store field/action (exact-bidirectional compat test green); `tool-presentation` maps `present_files` → 'deliver' variant; en/zh + cross-face contract updated.

| Command | Result |
| --- | --- |
| `cd ui && pnpm test src/components/chat/DeliverableCard.test.tsx src/lib/deliverable-download.test.ts src/lib/run-rows.test.ts src/lib/store.test.ts` | PASS 74/74 (download: sequential chunks → one objectURL+revoke with real bytes & CJK filename, abort mid-transfer closes `xfr_active` with no objectURL, changed/mismatch/missing typed reasons all close the transfer, preview decodes real bytes, isTextPreviewable matrix; card: 2 groups → `2 files delivered`, no 'Verification passed' string, failures+partial badge, verify→verified via RPC read+close, changed→explanatory state+retry, preview toggle; run-rows: presented event position, additive dedupe dvs-1/dvs-2, id-less payload → no card; store: cursor dedupe, one-active-download + cancel→unchecked+close, sha-mismatch retry → unavailable) |
| `pnpm typecheck` | PASS |
| `pnpm vitest run src/lib/ui-sdk-face-compat.test.ts src/i18n/completeness.test.ts` | PASS |
| `git diff --check` | clean |
| `just ci` | PASS on `6a0064f6` content: fmt-check clean; ui-ci green (tsc, vitest 434/434, vite build, i18n completeness 1424/1424 + cross-face 8/8); `go vet` clean; `go test -timeout 20m ./...` all packages ok (sdk/internal 225.2s, conformance 66.4s — digest unchanged at `00d1b7b4…`, no internal/ change); headless-compile ok; plugin-ci all ok. ci-generated `ui/src/generated/*` drift `git restore`d. |
| Real-browser smoke (split pair `go run ./cmd/vivy` :8787 + `pnpm dev` :3015) | PASS 6/6: transcript card `deliverable-dvs-smoke1` renders with unverified item; Verify flips to verified via real `deliverables/read` (digest matched); Preview shows actual workspace bytes incl. CJK (`第二行 CJK 内容`); Download lands `notes.txt` byte-identical (sha256 `dd2d33ce…` verified on disk, not a mocked callback); FilesPanel DELIVERY GROUPS lists the set and focuses the card via scrollIntoView; tampered file → Verify reports 'file changed since delivery'. Fixture: committed `deliverables.presented` event (payload_version 1) + real file in `~/.vivy/workspaces/<run_id>`. Recording `/home/ubuntu/screencasts/rec-8d9e54d6-*/` + 6 screenshots. |

Test-map notes: "no success card before committed event" is structural — the card only exists by folding committed `deliverables.presented` events in `foldContinuityRows`; "one active transfer per connection" is covered by the AbortController map test (second download call while one is in flight does not issue a second read chain). Object-URL revoke on unmount: the store revokes immediately after the anchor click inside `downloadDeliverable`'s finally, so there is no mounted-lifetime URL to leak; cancel/unmount abort is exercised through the store action. Long paths/CJK: download filename carries the committed item name (`报告.txt` path exercised in the unit test; CJK preview bytes verified in smoke). Screen-reader labels: `aria-live` state text + per-item `aria-label` assertions in the card test. Keyboard actions: all controls are native `<button>`s inside the card; mobile overflow delegated to the collapsible section layout. Postgres DSN-gated backend tests SKIP = unverified, not a pass.

## T12 — End-to-end coding acceptance and Lite

Scope delivered: `internal/app/continuity_e2e_test.go` drives the full continuity loop over the real app/RPC/services/stores on an isolated temp workspace — session A commits rationale + tool result, unrelated session C holds a sentinel string, session B runs `history_search`/`history_read`/`history_trace` → `reference_context` attach → `patch` on the source file → `bash tar` → `present_files`; the test replays the journal twice asserting receipt dedupe (no duplicate reference/group) and asserts no captured model request body contains the C sentinel. `ui/e2e/continuity-setup.ts` + `ui/playwright.continuity.config.ts` + `ui/e2e/session-continuity.spec.ts` run a real backend (`go run ./cmd/vivy`, Linux GO_EXE override, isolated `.continuity-workdir/` config/db/workspace — never tenant `data/`) + Vite split pair at `127.0.0.1:3015` against a deterministic loopback model (`127.0.0.1:8797`, OpenAI-compatible SSE, scripted FIFO replies, raw request capture). `recipes/lite.vivy.yml` composes the Lite Generation from 17 verified real modules (core loop/model/storage/checkpoint/credential/sandbox/tool-host + protected-tools + context/skill/observer/status/presentation/action hosts + context-source + provider-profiles); channels, persona/evolution/memory/notebook, mcp-host, channel-host and face-host are physically absent — the embedded web gateway/UI is app-owned and retained (no `vivy/web` invented, `vivy/face-host` not treated as a Web Face provider).

Contract wiring corrections (product fixes, not test bypasses): mid-run `deliverables.presented` / `context.reference_attached` events were journaled by `CommitContinuityOperation` but never published to `events.Bus`, so live subscribers missed them (cards only appeared on replay). `internal/runtime/hooks.go` adds a `withRunEventPublisher`/`publishCommittedRunEvent` ctx seam wired at the run ctx (`service.go`), resume ctx and tool ctx (`action_tool.go`); `deliverable_service.go`/`reference_service.go` publish `result.Events` when `NewlyCommitted`. Wire normalization: Go nil slices serialized `failures: null` — `normalizeDeliverySet` in `ui/src/lib/api.ts` and the `deliveryRow` fold in `run-rows.ts` now coerce items/failures to arrays at the boundary.

| Command | Result |
| --- | --- |
| `go test ./internal/app -run TestContinuityCodingLoop -count=1` | PASS 0.19s — full loop commit order, dedupe replay, C-sentinel absent from every captured model request |
| `cd ui && GO_EXE=$(which go) pnpm exec playwright test --config playwright.continuity.config.ts` | PASS 4/4 (11.4s): (1) card live + verify→verified + preview real bytes + download sha256 byte-exact + sentinel absent; (2) tamper→'file changed since delivery', second same-path group partial ('1 failed', ghost.txt missing), reload → still exactly 2 cards; (3) picker attach → ref card + saved excerpt retained after source session deletion, `source_unavailable` badge + disabled source-jump, no duplicate after replay; (4) 320/768/1280 widths screenshots + keyboard focus/Enter download of second-group item, sha256 = `SECOND-BYTES\n` |
| `go run ./sdk pack --recipe recipes/lite.vivy.yml --output /tmp/lite-artifact` | PASS — generationId `ae3a4bf36cb986401adc679fc7bf86572548dbe7fabb7c2955300923c8798e5f`, recipeDigest `001d371e1b1b973386c4abf920e83e5502dacaa6e9dfc51c9c787536d21ab118`, internal source sha `c3be89691a8958b5626f309cdfab6e35c139314fff3f853dfc839e4e5c3c384c` |
| `go run ./sdk inspect-artifact /tmp/lite-artifact` | PASS — 17 modules listed; channel/face/mcp hosts, dingtalk/discord/feishu/qq/telegram, persona/evolution/memory/notebook physically absent from manifest + generated `zz_assembly.go` (SSH/image-offload have no real modules anywhere — nothing to omit); `ui.sdkVersion 1.0.0` retained with zero extensions |
| Lite HTTP + GUI smoke (`/tmp/lite-artifact/vivy` on `127.0.0.1:8899`, isolated `/tmp/lite-home`) | PASS — `GET /` 200 `text/html` (`<title>VIVY</title>` + asset link), `GET /assets/index-*.js` 200; `tools/list` over real `/rpc/bootstrap` + websocket exposes `history_search`, `history_read`, `history_trace`, `reference_context`, `present_files` plus fs/shell tools — all discoverable on the packed Lite binary |
| `go test ./sdk/internal/conformance -run TestCheckedInProviderConformanceMatchesExecutedSuites -count=1` | PASS 51.9s — internal digest refreshed `00d1b7b4…` → `c3be89…` (runtime contract-wiring edits) and written back to `sdk/internal/assembly/conformance_results.json` (5 internal suites) |
| `go test ./sdk/internal/conformance -run 'TestGeneration(FailureMatrixEvidence\|RollbackRestoresCatalogAndLocaleIdentity)' -count=1` | PASS |
| `go test ./sdk/internal -run 'Test(GenerationFailureMatrixExecutesEveryCase\|MinimalArtifactPhysicallyOmitsOptionalModules)' -count=1` | PASS 32.0s (default recipe also packs + eval-boots inside the matrix — default generation unchanged) |
| `git diff --check` | clean |
| `just ci` | PASS on T12 content: fmt-check clean; ui-ci green (tsc, vitest 434/434, vite build, i18n completeness + cross-face 8/8); `go vet` clean; `go test -timeout 20m ./...` all packages ok (internal/app 2.98s, internal/runtime 26.3s, internal/rpc 9.5s, sdk/internal 212.5s, conformance 64.7s); headless-compile ok; plugin-ci all ok. ci-generated `ui/src/generated/*` drift `git restore`d. `ui/.gitignore` gained `.continuity-workdir/` (generated e2e workdir) |

Evidence collected (journal of the e2e workdir `ui/.continuity-workdir/state/continuity.db`): runs `run_e0e9e4ba90a8e590` (delivery loop), `run_0141790d233e1d6b` (second same-path group), `run_58e9150330c69fa7` (attach+use) all `completed`; event counts 2×`deliverables.presented`, 1×`context.reference_attached`, 5×`tool.requested/started/finished`, 3×`model.request/completed`; receipts `deliverable.present`×2 (tool-call ids `call-3`,`call-5`) + `run.admission`×1. Inspected generation id `ae3a4bf3…` above. Model request bodies asserted free of `SENTINEL-UNRELATED-SESSION`.

Disclosure notes: (a) live-vs-replay event publication is a real contract wiring fix required by the spec's "card appears live" behavior — journaled single-source-of-truth unchanged; (b) source-deletion state is asserted via the actual badge text `source_unavailable` + disabled `View source` + retained 'Saved excerpt' block — the spec's quoted sentence is a paraphrase, not a literal i18n key; (c) each run owns a private workspace, so the second same-path group writes `report.txt` in run-2's workspace first (otherwise both items fail — run-1's file is not visible to run-2); (d) no local PostgreSQL — DSN-gated tests SKIP (unverified, not a pass); (e) `tools/list` on Lite also surfaces first-party tools beyond the recipe modules (they are runtime-owned legacy inventory, not `std/tool@v1` providers) — consistent with the sole ToolHost rule, recorded here rather than trimmed.
