# Acceptance

1. Dashboard sectioning matches the Settings page: page title + Tabs + Card with a description.
2. The three numbers no longer each occupy a large card; they are grouped in a "Runtime Status" block.
3. Token and Audit are not both expanded on the first screen; click the corresponding Tab.

`just ci` remains the gate.
