# Verification — FACE-PACK §14 decision record

```
just ci        # docs-only slice; run through the product gate
CI-EXIT:0
```

Decision source: the user selected each of the four questions in-session (all recommended
values: standalone `go.mod` / always web / no cohabitation in the first cut / headless fail
exit). Contract text §14 was rewritten in place as a "decided" record, dated 2026-09-02.
