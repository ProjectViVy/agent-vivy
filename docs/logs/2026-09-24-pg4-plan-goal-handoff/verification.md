# Verification

- Red check: the first focused RPC run failed because `plan/enter` left the active Goal unchanged at WorkVersion 2. A second red check showed a retry could enter Plan while the paused Goal run was still alive.
- Focused scenario tests: `go test ./internal/rpc ./internal/runtime -run 'TestHumanPlanEntryDrainsGoalBeforeBecomingEffective|TestDeleteDuringGoalDrainCannotResurrectPlan|TestPlanEntryDrainCancellationLeavesGoalPausedAndPlanIneffective|TestReplayedStartGoalDecisionDoesNotRearmAfterRestart|TestPlanDecisionHandoffUsesOriginTerminalBeforeGoalAdmission' -count=1 -timeout=60s` passed.
- Focused race check with the same package and test filter passed. The first race attempt with a 50 ms drain deadline exposed a test timing assumption: Windows SQLite under race instrumentation could expire before the durable pause. The test now cancels only after observing the persisted pause and checks the retry remains blocked by the live run.
- Full package race check: `go test -race ./internal/runtime ./internal/rpc ./internal/tools -count=1 -timeout=5m` failed. It reproduced the already-baselined pinned Eino v0.9.13 `compose.ToolsNode.Stream` write race at `tool_node.go:1253`. Runtime and RPC also reached the five-minute package timeout under Windows SQLite race instrumentation; `internal/tools` passed. The focused handoff race suite passed separately.
- Internal Provider source evidence: `go run ./sdk/internal/cmd/source-hash internal ''` returned `b27f5f522e1b9d5a75ca869d5f9d047aca4d9510cb7f43a9d5c8c468659ac536`, applied to the five internal Provider conformance entries.
- First stable-tree `just ci`: passed exit 0 (UI typecheck, 400 tests, build and i18n; vet, all Go packages including runtime/RPC and SDK conformance, headless compile, plugin/face gates). A later focused red test exposed replay rearming. The final-tree `just ci` rerun also passed exit 0 with the same gates.
- Exact plan race command, with no timeout override and no concurrent test job: `go test -race ./internal/runtime ./internal/rpc ./internal/tools -count=1` exited 1. `internal/rpc` and `internal/tools` passed; `internal/runtime` failed on the previously baselined Eino v0.9.13 `compose.ToolsNode.Stream` write race at `tool_node.go:1253`. It completed without a package timeout. No Eino change was made.
- Docker-required and live PostgreSQL checks: deferred by the user. No Docker activity occurred. Eino remains pinned at v0.9.13; no upgrade was attempted.

## Review correction round 1

- Behavioral RED: `go test ./internal/runtime -run TestResumeBeforePlanCancellationLeavesOwnedRunAlive -count=1 -timeout=30s` failed with the resumed owned run `cancelled`. A test-only WorkStore barrier forced a competing durable resume after `goal.paused` persisted and before the old unconditional cancellation. The final deterministic test uses the two real production Plan phases with `ResumeGoal` between them; the temporary barrier was removed.
- Focused GREEN after the production phase split: `go test ./internal/runtime ./internal/rpc -run 'TestResumeBeforePlanCancellationLeavesOwnedRunAlive|TestResumeCannotCommitAfterPlanClaimsCancellation|TestPlanEnterUsesAuthenticatedPeerSessionBinding|TestStartGoalDecisionRPCWakesExactlyOneRound' -count=1 -timeout=60s` passed. The same filter with `-race` and no timeout override passed. These tests check persisted Work, ordered Journal events, owned run status, real connection identity, and RPC-triggered Goal admission.
- An unfiltered diagnostic `go test ./internal/runtime ./internal/rpc ./internal/tools -count=1` failed once on the unrelated timing-sensitive `TestServiceAgentsMDInjectionSurvivesApprovalResume` (`approval is not durable yet`). Its immediate isolated rerun passed. This was not the required `just ci` gate.
- The pre-phase-split `just ci` passed exit 0, including all UI, Go, SDK, headless, plugin, and face gates. It is not final-tree evidence; the required final-tree rerun follows the phase split.
- Internal Provider source evidence after the final code edit: `go run ./sdk/internal/cmd/source-hash internal ''` returned `b09efedd4ca152ba7ac66d77119ac4d1eea31f8d1f3079c6cdf3b313fe45dbfc`, applied to all five existing internal Provider conformance entries.
- Final production-tree `just ci`: passed exit 0, including runtime (499.326s), RPC (323.099s), SDK/internal (695.724s), UI, vet, headless, plugin, and face gates. The prior isolated approval fixture failure did not recur.
- Read-only review found that the first `start_goal` RPC test used a nil Peer and could pass through the origin terminal wake alone. The test-only correction now sends `plan/decide` over a real host-authenticated Peer pair bound through `session/get`, rejects a mismatched existing session with `CodeConflict` and no Work mutation, and holds the origin before terminal cleanup. A Goal-driver active-Goal ReadWork probe must fire before origin release, proving the initial RPC wake independently of terminal cleanup. `go test ./internal/runtime -run '^TestStartGoalDecisionRPCWakesExactlyOneRound$' -count=1 -timeout=45s` and the same command with `-race` (without timeout override) both passed.
- The source digest includes test source; after this test-only amendment `go run ./sdk/internal/cmd/source-hash internal ''` returned `aaf5b3550390d43081adcddf2fb6373c786b740462bdfb462cf0f9b371f2d166`, applied to the five existing conformance entries. No production runtime or RPC file changed after the passing `just ci`.
- Targeted digest producer gate after the test-only amendment: `go test ./sdk/internal/conformance -run '^TestCheckedInProviderConformanceMatchesExecutedSuites$' -count=1 -timeout=10m` passed (121.434s). The exact unfiltered full-race command remains deferred pending the scoped read-only review; no concurrent test job was run.
- Final test-only review correction: replay previously ran before the wake probe, so replay's own `DecidePlan.ReadWork` could satisfy it. The probe now requires the detached Goal-driver background context, and the test observes it while origin remains active **before** sending identical replay. Focused normal `go test ./internal/runtime -run '^TestStartGoalDecisionRPCWakesExactlyOneRound$' -count=1 -timeout=45s` and focused `-race` without timeout override both passed. New source digest: `e3ea80e554b8bc914f7c168c0b0fa61f2cd9a02f16b39fcd477f0419d38a3173` in the same five entries. Targeted `go test ./sdk/internal/conformance -run '^TestCheckedInProviderConformanceMatchesExecutedSuites$' -count=1 -timeout=10m` passed (167.173s).
