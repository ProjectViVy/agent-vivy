# Acceptance

From the repository root, run:

```powershell
just tui
```

The command must start the real VIVY CODE TUI in the current project. It must not select `--demo` and must not invoke Studio.
