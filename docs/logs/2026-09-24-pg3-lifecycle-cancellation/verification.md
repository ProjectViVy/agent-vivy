# Verification

- TDD RED — `go test ./internal/rpc -run TestWorkViewDisarmsDurablyPausedOrClearedGoalDuringCancellation -count=1`: failed as intended before production edits. Both paused and cleared durable Goal states projected `activation: armed` and the cancelling RunID. After the three-line WorkView correction, the same test passed.
- Existing-behavior checks — the new Service/storage tests for reserved candidate invalidation, stale-revision report rejection, pending approval/question, cancellation, round cap, and crash recovery passed without further production edits. The stale-report test initially reached approval handling instead of the tool; its fixture was corrected to use the existing auto-approved path and now requires a persisted `tool.finished` stale-reference result.
- Focused race — `go test -race ./internal/runtime ./internal/rpc -run 'TestGoalReservationCannotCommitAfterPauseOrEdit|TestOldRevisionGoalRunCannotReportIntoEditedGoal|TestPendingGoalQuestionKeepsSessionBusy|TestPendingGoalApprovalKeepsSessionBusy|TestCancelGoalRunDoesNotImmediatelyReplaceIt|TestGoalRoundCapPersistsExplicitBlock|TestCrashAfterGoalRunCommitNeverRefundsOrReplaysRound|TestWorkViewDisarmsDurablyPausedOrClearedGoalDuringCancellation|TestPauseAndClearReturnDisarmedWorkViewWhileOwnedRunIsCancelling' -count=1`: PASS, exit 0 (`internal/runtime` 23.346s; `internal/rpc` 4.252s), no race reports. A prior race attempt caught a test setup timing issue: the storage notifier preceded Service's installation of the cancellable RunID. The test now waits for the actual RunID in `GoalActivation` before cancelling.
- Durable evidence — tests inspect `ReadWork`, `ReplayWork`, run rows/status, user messages and RunIDs, pending approval/question rows, and the old report's `tool.finished` result. The RPC integration test holds the cancelling run in memory and verifies both mutation and subsequent `session/work/get` views while `GoalActivation` still reports the old RunID. Work subscription publishes event metadata only, with no activation projection.
- Source digest — `go run ./sdk/internal/cmd/source-hash internal ''` produced `ea317245022da5e44232863062fb5a2ac123af5bd92d15eaff9f5db2134a61a8`. Five matching checked-in SDK conformance entries were refreshed.
- Final stable-tree gate — PowerShell prepended `C:\Program Files\Go\bin` to PATH, then ran `just ci`: PASS, exit 0. UI typecheck, 49 test files / 400 tests, build and i18n passed; `go vet ./...` passed; `go test -timeout 20m ./...` passed, including `internal/runtime` 465.534s, `internal/rpc` cached, `sdk/internal` 720.186s, and `sdk/internal/conformance` 324.496s; headless compile and plugin/face vet/tests passed. The Vite chunk-size advisory and expected React test stderr were non-failing. An earlier full gate was interrupted after the stale-report fixture changed, so only this final run certifies the delivered tree.
- Diff review — `git diff --check` passed, with line-ending warnings for the unrelated modified generated UI files and the JSON evidence file. The Task 2 staged paths are checked again before commit.

Pinned Eino v0.9.13 source inspection confirmed ADK Runner `Run`/`ResumeWithParams`, `components/tool.StatefulInterrupt`, and `compose.GetToolCallID`; the existing Vivy runtime uses those APIs for suspension, exact resume, and tool-call identity. No Eino import escaped `internal/runtime`/`internal/provider`, and no new loop or ledger was added. No live PostgreSQL DSN or browser/model walkthrough was supplied for this Task 2 slice; those downstream acceptance gates are not claimed here.
