# Acceptance

1. 打开宽 TUI：输入框下一行左侧是 permission · model · provider，右侧是 `shift+h 帮助` 和 `ctrl+x 快捷`；底栏不再出现 `TUI` 或 `live · 地址`。
2. 右栏出现 `host · <网关地址>`，不再重复 model/provider/permission。
3. 空输入按 `shift+h` 打开标题为「帮助」的命令面板。
4. `ctrl+x` 仍打开「快捷方式」；`esc` 关掉。
