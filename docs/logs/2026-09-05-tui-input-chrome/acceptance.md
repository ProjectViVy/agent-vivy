# Acceptance

1. Open a wide TUI: below the input, the left side shows permission · model · provider, and the right side shows `shift+h help` and `ctrl+x shortcuts`; the footer no longer shows `TUI` or `live · address`.
2. The right column shows `host · <gateway address>` and no longer repeats model/provider/permission.
3. With empty input, press `shift+h` to open the command palette titled “Help”.
4. `ctrl+x` still opens “Shortcuts”; `esc` closes it.
