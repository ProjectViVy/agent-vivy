# Verification

- Behavioral RED: `& 'C:/Program Files/Go/bin/go.exe' test ./internal/rpc -run TestWorkSubscriptionBuffersCommitAfterCapturedWatermark -count=1` failed because the subscription response lacked `watermark_seq` (`<nil>`, wanted durable WorkSeq 1). Bare `go` was initially absent from this shell's PATH; the installed toolchain at `C:/Program Files/Go/bin/go.exe` was used for the valid RED and later checks.
- UI RED: `pnpm exec vitest run src/lib/work-subscription.test.ts` failed because reconnect did not call the WorkView refresh callback. `pnpm exec vitest run src/lib/store.test.ts -t 'replaces armed activation|opens a committed automatic run|discards a previous session WorkView'` failed on the absent reconnect callback and automatic run opening; the old-session response case already passed.
- Final focused Go: `& 'C:/Program Files/Go/bin/go.exe' test ./internal/rpc -run 'TestWork(Subscription|View)' -count=1` passed. The same selector with `test -race` passed. The interleaving test checks committed SQLite WorkVersion 2 and paused Goal state, the replayed sequence and buffered later sequence each once, and notification epoch/version metadata. A second handler over the same backend reports a new process epoch at the same durable version.
- Existing real driver restart coverage: `& 'C:/Program Files/Go/bin/go.exe' test ./internal/runtime -run '^TestGoalRecoveryKeepsPendingQuestionDisarmed$' -count=1` passed. It recovers durable Goal state into a new Service and observes disarmed process activation.
- Final focused UI: `pnpm exec vitest run src/lib/store.test.ts src/lib/work-subscription.test.ts` passed, 39 tests. The reconnect test verifies refresh precedes resubscription and duplicate session sequence is delivered once. Store tests verify the old armed view is replaced by disarmed backend WorkView, the automatic run opens once, and a previous session response is discarded.
- `pnpm typecheck` passed on the final Task 1 tree. The staging prehook ran with Go on PATH. The two pre-existing user-owned generated UI files were backed up and restored with matching SHA-256 hashes after each project typecheck run; neither is staged. A temporary `pnpm exec tsc --noEmit` diagnostic exposed the same structural type errors before the final WorkView/WorkCommitView split.
- At base `b2a7b82443ba66c845a0c97bed5ab9b82c38f5ed`, exact `pnpm typecheck` passed in a detached diagnostic worktree. The intermediate Task 1 tree failed two FaceClientStore compatibility assertions because a required process epoch had been placed on the shared store state and then its shared mutation result. A temporary direct assignment identified each incompatible field. The final wire-only `WorkView` and `WorkCommitView` types require the epoch on fresh RPC responses while the shared store contract retains its SDK-compatible shape; exact `pnpm typecheck` then passed.
- `& 'C:/Program Files/Go/bin/go.exe' run ./sdk/internal/cmd/source-hash internal ''` returned `8d08e28b62a7ec6ea0dce6759126da371fbccadcb96c00f0028e9f4c8ab3ed0e`; the five internal Provider conformance digests were updated. The long producer gate and `just ci` are reserved for the integrated PG-5 run after Task 2. No Docker, PostgreSQL, browser, or external mutation was run for this focused slice.
- `git diff --check` passed for Task 1 paths. The detached base worktree was deregistered. Its temporary directory remains at `C:/Users/Administrator/Desktop/morediva/diva-go/agent-vivy-pg5-basecheck`: automatic approval review rejected the verified-target PowerShell recursive cleanup command without a more specific reason.
