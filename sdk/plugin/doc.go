// Package plugin marks the public plugin SDK window.
//
// Plugin implementations may depend on sdk/plugin and other exported SDK
// packages, but the import firewall forbids dependencies on agent-vivy/internal.
package plugin
