package generation

// EmbeddedManifestBase64 is replaced by vivy-sdk pack through a Go overlay.
// A packed executable therefore carries the exact sealed Manifest it reports.
var EmbeddedManifestBase64 string
