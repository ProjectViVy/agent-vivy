{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-vivy.local/schemas/events/payloads/child.resumed.json",
  "title": "child.resumed payload (v1)",
  "type": "object",
  "properties": {"approval_id": {"type": "string", "minLength": 1}, "decision": {"type": "string", "enum": ["approved", "denied"]}},
  "required": ["approval_id", "decision"],
  "additionalProperties": false
}
