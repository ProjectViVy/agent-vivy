# Placeholder so go:embed all:dist compiles before the first Vite build.
# The real build output (index.html, assets/) is generated by `npm run build`
# and ignored by git; vite.config.ts keeps emptyOutDir off to protect this file.
